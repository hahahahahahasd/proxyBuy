# Use the official Nginx image from Docker Hub
FROM nginx:alpine

# Copy the built frontend assets into the Nginx HTML directory
# IMPORTANT: You must build the frontend locally first!
# Run: npm run build -w packages/customer-app
COPY ./packages/customer-app/dist /usr/share/nginx/html

# Copy our custom Nginx configuration
COPY ./nginx/nginx.conf /etc/nginx/conf.d/default.conf
